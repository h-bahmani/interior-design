# خلاصهٔ کارهای انجام‌شده روی InteriorAI

این سند خلاصهٔ فنی همهٔ کارهایی است که در این نشست روی پروژهٔ InteriorAI (سامانهٔ هوشمند بازطراحی داخلی با هوش مصنوعی مولد) انجام شد — برای استفاده در تهیهٔ اسلایدهای ارائهٔ پروژه.

## ۱. معرفی کلی پروژه

InteriorAI یک اپ وب است که با یک عکس از یک اتاق واقعی، کاربر می‌تواند:
- سبک اتاق را با ۱۶ سبک از پیش‌تعریف‌شده یا یک توصیف متنی آزاد عوض کند
- فقط رنگ دیوارها/دکور را تغییر دهد بدون تغییر چیدمان
- یک ناحیهٔ خالی را با اثاثیهٔ جدید مبله کند
- یک شیء را ویرایش یا حذف کند
- یک شیء را از روی عکس مرجع (مثلاً یک صندلی خاص) داخل اتاق قرار دهد
- رنگ یک شیء یا سطح خاص را عوض کند
- یک بافت/الگو (مثلاً کاغذ دیواری) را روی یک سطح اعمال کند

**معماری فنی:** دو مدل تولید تصویر (SD1.5 «سریع» / SDXL «کیفیت») + ControlNet (حفظ ساختار) + IP-Adapter (شیء از روی عکس مرجع) + YOLOv8 و SAM و SegFormer (تشخیص و تفکیک اشیا) + LaMa (حذف شیء) + OpenCV خالص (تغییر رنگ و بافت، بدون مدل مولد).

## ۲. کارهای این نشست — به ترتیب انجام

### ۲.۱ تکمیل گزارش نهایی پروژه (Word)
گزارش ۱۸ فصلی که توسط منتور تهیه شده بود، با اطلاعات واقعی از کد و معماری پروژه پر شد (تمام ۱۴۶ جای خالی)، شامل توضیح مدل‌ها، پارامترها، معماری، منابع، مجوزها، و روش ارزیابی.

### ۲.۲ افزودن انیمیشن «قبل/بعد» با خروجی ویدیو
یک قابلیت جدید در نتیجهٔ نهایی: نمایش انیمیشن گذار بین عکس اصلی و نتیجه (چهار حالت: محو‌شدن، جاروب، زوم، اسلاید) با امکان دانلود به‌صورت فایل ویدیوی واقعی (webm) — کاملاً سمت مرورگر، بدون هیچ هزینهٔ پردازشی/مدل.

### ۲.۳ رفع باگ‌های بحرانی بک‌اند

| باگ | علت | راه‌حل |
|---|---|---|
| خطای دانلود مدل SDXL (Juggernaut XL) | مدل روی HuggingFace فقط نسخهٔ fp16 را منتشر کرده؛ کد بدون `variant="fp16"` دنبال فایل پیش‌فرض می‌گشت که اصلاً وجود نداشت | اضافه‌شدن `variant="fp16", use_safetensors=True` به هر سه مؤلفهٔ SDXL |
| گیر کردن حالت مدل بعد از شکست لود | اگر لود مدل Quality شکست می‌خورد، `_current_model["name"]` هنوز مقدار قدیمی (Fast) را نگه می‌داشت، پس تلاش بعدی برای Fast اصلاً دوباره لود نمی‌شد | ریست شدن وضعیت به `None` قبل از هر تلاش لود |
| **سرریز ۷۷ توکنی CLIP** (مهم‌ترین یافته) | اندازه‌گیری با توکنایزر واقعی: پرامپت‌های سبک به ۱۴۷ تا ۱۸۰ توکن می‌رسیدند در حالی که سقف مدل ۷۷ توکن است — یعنی نیمی از دستورات (از جمله «همین اتاق را حفظ کن») بی‌سروصدا حذف می‌شد | کوتاه‌سازی همهٔ ۱۶ پرامپت سبک، حذف تکرار سه‌گانهٔ عبارات کیفیت، و تغییر ترتیب که قیدهای حیاتی همیشه اول پرامپت بیایند نه آخر |
| صندلی معلق/تکراری در نتایج | چند سبک صراحتاً «floating shelves» می‌گفتند که مدل را به سمت اشیای معلق سوق می‌داد | نگاتیو عمومی جدید (`floating furniture, duplicate chairs, ...`) + تغییر واژگان سبک‌ها |
| خطای «Invalid image» روی عکس‌های عادی گوشی | فرانت فقط نوع فایل و حجم را چک می‌کرد، نه ابعاد پیکسلی؛ یک عکس ۲۰-۵۰ مگاپیکسلی معمولی گوشی از همهٔ چک‌ها رد می‌شد و فقط در بک‌اند (سقف ۱۶ مگاپیکسل) رد می‌شد | تغییر اندازهٔ خودکار سمت مرورگر قبل از آپلود (حداکثر ۲۴۰۰ پیکسل ضلع بزرگ) |
| «Add Object From Photo» نتیجهٔ نامرتبط می‌داد | تنها ابزار پروژه بود که هیچ محدودهٔ مکانی نمی‌گرفت — کل عکس ۷۶۸×۷۶۸ اتاق یک‌جا بازتولید می‌شد و تأثیر عکس مرجع باید با کل صحنه رقابت می‌کرد | افزودن مرحلهٔ انتخاب محل (مثل بقیهٔ ابزارها) + تولید محدود به یک برش کوچک اطراف همان محل، سپس ترکیب با فیدرینگ در عکس اصلی |
| تشخیص اشیا کم می‌گرفت/تشخیص نمی‌داد | مدل SegFormer نسخهٔ سبک (b0) بود که خیلی از سطوح رایج داخل اتاق (پرده، کوسن، کابینت) را که در ۸۰ کلاس YOLO نیستند تشخیص نمی‌داد؛ آستانهٔ حداقل اندازه هم اشیای کوچک را حذف می‌کرد | ارتقا به SegFormer b2، کاهش آستانهٔ اندازه از ۰.۳٪ به ۰.۱۲٪، و افزودن چند پیکسل گسترش (dilation) به هر ماسک تشخیصی |

### ۲.۴ رفع مشکلات رابط کاربری
- برچسب اشتباه «۸ Design Styles» که در واقعیت ۱۶ سبک وجود داشت (رشتهٔ هاردکد فراموش‌شده)
- نامرتبی ظاهری دکمهٔ Fast/Quality در هدر (اندازهٔ متفاوت از بقیهٔ دکمه‌ها)
- راهنمای واضح زیر «Custom Prompt» که توضیح دهد این حالت برای تغییر کل سبک اتاق است، نه ویرایش یک شیء خاص

### ۲.۵ قابلیت‌های جدید افزوده‌شده
- **پیش‌نمایش آنی تغییر رنگ:** قبل از اعمال واقعی رنگ (که نیاز به فراخوانی مدل و انتظار دارد)، یک پیش‌نمایش فوری در خود مرورگر با همان الگوریتم رنگی بک‌اند (تبدیل فضای رنگ LAB) نمایش داده می‌شود — بدون هیچ پردازش سمت سرور.
- **اعمال بافت/الگو (کاغذ دیواری و مشابه):** قابلیت جدید کنار تغییر رنگ — کاربر یک نمونه بافت/الگو آپلود می‌کند، سیستم آن را روی سطح انتخابی کاشی‌کاری می‌کند و با نور واقعی همان قسمت از عکس ترکیب می‌کند (سایه‌ها و نورهای واقعی حفظ می‌شوند) — کاملاً OpenCV، بدون مدل مولد.

### ۲.۶ زیرساخت: تلاش برای رفع وابستگی دائمی به Colab
مشکل اصلی مطرح‌شده: هر چند ساعت نشست Colab رایگان قطع می‌شود و باید دستی دوباره راه‌اندازی شود.
- بررسی شد: میزبانی رایگان روی Hugging Face Spaces با فناوری ZeroGPU — در عمل به دلیل یک باگ فعلی و حل‌نشدهٔ خود Hugging Face (تأییدشده در انجمن رسمی‌شان) برای این اکانت در دسترس نبود.
- یک نسخهٔ کامل از بک‌اند برای این مسیر (`backend/hf_space/app.py`، معماری FastAPI روی Gradio) از قبل آماده و در ریپو موجود است، برای روزی که این مسیر باز شود.
- مسیر جایگزین انتخاب‌شده: RunPod Serverless (هزینهٔ واقعی نزدیک صفر، بدون بازنویسی کد Flask فعلی) — در مراحل بعدی راه‌اندازی می‌شود.

### ۲.۷ ارزیابی کمّی
یک طرح کامل اجرای خودکار (شش سلول در نوت‌بوک) اضافه شد که برای مجموعه‌ای از تصاویر آزمون، زمان اجرا، حافظهٔ مصرفی، نرخ موفقیت و میزان تغییر پیکسل بیرون از ماسک انتخابی را به‌صورت خودکار ثبت می‌کند، به‌همراه یک برگهٔ امتیازدهی «کور» (بدون افشای نام مدل/سبک به ارزیاب) برای بخش ارزیابی انسانی کیفیت.

## ۳. روش کار در این نشست (برای بخش «روش» ارائه، در صورت نیاز)

هر تغییر فنی قبل از commit شدن با یکی از این روش‌ها تأیید می‌شد:
- **برای منطق خالص پایتون/OpenCV (بدون نیاز به GPU):** اجرای واقعی و مستقیم با دادهٔ مصنوعی و بررسی عددی نتیجه (مثلاً الگوریتم بافت با یک دیوار شبیه‌سازی‌شده تست شد).
- **برای منطق فرانت‌اند (React/JS):** اجرای زنده در مرورگر واقعی روی سرور توسعهٔ در حال اجرا، با بررسی DOM، پیام‌های کنسول و مقادیر پیکسلی canvas.
- **برای منطق مدل‌های تولید تصویر (نیازمند GPU واقعی):** از آنجا که این محیط به GPU دسترسی ندارد، این تغییرات صرفاً بر اساس مستندات رسمی و شواهد قوی (مثل شمارش دقیق توکن با توکنایزر واقعی CLIP) نوشته شدند، و صادقانه به‌عنوان «نیازمند تأیید نهایی روی بک‌اند زنده» علامت‌گذاری شدند.

## ۴. محدودیت‌های شناخته‌شده (صادقانه، برای بخش «محدودیت‌ها»)
- اعمال بافت فاقد اصلاح پرسپکتیو است؛ برای دیوارهای زاویه‌دار نتیجهٔ کامل نمی‌دهد.
- تغییر اندازهٔ یک شیء (مثلاً بزرگ‌کردن تلویزیون) در هیچ‌کدام از ابزارها پشتیبانی نمی‌شود.
- ارزیابی کمّی هنوز به‌صورت واقعی روی یک بک‌اند فعال اجرا نشده (طرح آماده است).
- کیفیت نهایی چند فیکس این نشست (مربوط به مدل‌های تولید تصویر) هنوز روی بک‌اند زنده تأیید نشده.
