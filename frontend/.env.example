# API v2 notebook URL (or a proxy that forwards every API v2 route).
REACT_APP_API_URL=https://your-notebook.ngrok-free.app
# For a local notebook API use http://localhost:7860.
# Restart the dev server / rebuild after changing this value.
# Firebase credentials remain in src/firebase.js, as in the original project.
# The CONNECTION_KEY printed by the notebook's last cell is entered in the
# app's "Connect AI Backend" modal, not here — it's stored in localStorage.
